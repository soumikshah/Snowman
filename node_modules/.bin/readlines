#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../readlines/bin/readlines" "$@"
  ret=$?
else 
  node  "$basedir/../readlines/bin/readlines" "$@"
  ret=$?
fi
exit $ret
